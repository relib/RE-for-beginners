\mysection{Еще кое-что о пустой ф-ции}

Вернемся к примеру с пустой ф-цией \myref{empty_func}.
Теперь, когда мы уже знаем о прологе и эпилоге ф-ции,
вот эта пустая ф-ция \myref{lst:empty_func} скомпилированная неоптимизирующим GCC:

\lstinputlisting[caption=\NonOptimizing GCC 8.2 x64 (\assemblyOutput),style=customasmx86]{patterns/016_empty_redux/1.s}

Это \INS{RET}, но пролог и эпилог ф-ции, вероятно, не был соптимизирован и остался как есть.
\INS{NOP} это, похоже, артефакт компилятора.
Так или иначе, единственная здесь рабочая инструкция это \INS{RET}.
Остальные инструкции могут быть убраны (или соптимизированы).

